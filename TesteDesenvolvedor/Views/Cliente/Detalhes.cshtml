@model TesteDesenvolvedor.Models.Cliente

@{
    ViewData["Title"] = "Detalhes do Cliente";
}

<h2>Detalhes</h2>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Nome)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Nome)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Email)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Email)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.CPF)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.CPF)
        </dt>
    </dl>
</div>
<a asp-action="Editar" asp-route-id="@Model.Id" class="btn btn-primary btn-sm">Editar</a>
<a asp-action="Index" class="btn btn-danger btn-sm">Voltar</a>